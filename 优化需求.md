src文件中是一个rag检索的项目。我现在想让你充分理解这个项目的流程和代码特点。把这些内容结合到我自己的项目（后端工程在backend目录）中来。

业务场景不一样的点：
参开项目（src）中是针对不同企业的文档进行检索，所以它设置了对应不同公司的路由，然后进行检索。但是我自己的项目不需要这个路由，我本地存储或者上传了很多文档，我就是想直接对这些文档进行检索即可。

注意以下技术栈。
• 使用DashScope API KEY（我的项目应该已经实现了），替换原有的openai API
• 可以使用qwen-plus作为LLM， text_embedding_v4作为embedding模型
• 使用 MinerU替换现有的docling解析
• 所有的参数还是需要统一配置。